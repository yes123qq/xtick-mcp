[project]
name = "xtick-mcp"
version = "0.1.0"
description = "Read-only XTick market data MCP server for FastMCP/Horizon"
requires-python = ">=3.11"
dependencies = [
  "fastmcp>=2.0,<4",
  "httpx>=0.27,<1",
]

[tool.uv]
package = false

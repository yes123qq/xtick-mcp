# Public default: no shared XTick token. This file is not loaded automatically.
XTICK_CREDENTIAL_MODE=byok
XTICK_BASE_URL=https://api.xtick.top
MCP_TRANSPORT=http
HOST=0.0.0.0
PORT=8000
# Private/local process only: explicitly change credential mode to private
# and supply XTICK_TOKEN through your own local secret mechanism.
